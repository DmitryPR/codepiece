# Dev Compose only: `better-sqlite3` compiles with node-gyp (Python + C++ toolchain).
FROM oven/bun:1
RUN apt-get update \
  && apt-get install -y --no-install-recommends python3 make g++ \
  && rm -rf /var/lib/apt/lists/*
WORKDIR /app
